1. Fix readTicketCount to handle empty string (retry reading)
2. Fix distributed mutex algorithm to include lockHeldByLocal and proper backoff.
3. Restructure processWithMutex and forwardBuy to separate the local bottleneck (300ms) from the distributed critical section (which has a 30ms window to force race conditions).
